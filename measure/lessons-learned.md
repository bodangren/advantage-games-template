# Lessons Learned

- Keep cartridge rules pure so an LLM can modify and test them without a browser.
- Treat direct-copy compatibility as a contract, not a late packaging task.
- Semantic edition values prevent cartridge behavior from depending on fixture names.
