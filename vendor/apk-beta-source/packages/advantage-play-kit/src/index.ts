/** Core APK runtime exports. */
export * from "./runtime/index.js";

/** Audience edition and semantic asset exports. */
export * from "./editions/index.js";
/** Filesystem-first standard asset-library exports. */
export * from "./assets/index.js";

/** T10-accepted capability shared cores (the seven accepted systems). */
export * from "./systems/index.js";

/** Accepted-inputs, blocked-scopes, and legacy-edition guards. */
export * from "./guards/index.js";

/** Cartridge manifest schema, scaffold generator, and exemplar. */
export * from "./scaffolding/index.js";

/** Versioned developer-kit API and compatibility plan. */
export * from "./compatibility/index.js";

/** Structured diagnostics for blocked scopes and unsupported capabilities. */
export * from "./diagnostics/index.js";

/** Normative compact/wide responsive composition and transition APIs. */
export * from "./responsive/index.js";

/** Accessible standard game presentation components and semantic DOM equivalents. */
export * from "./presentation/index.js";

/** Validated QC controls, performance probes, and browser-test adapters. */
export * from "./qc/index.js";
